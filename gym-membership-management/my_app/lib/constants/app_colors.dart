import 'package:flutter/material.dart';

class AppColors {
  // Deep Dark Background (Revolut Business Style)
  static const Color background = Color(0xFF06090E); 
  
  // Surface / Card background
  static const Color cardBackground = Color(0xFF13181F);
  
  // High contrast text
  static const Color primaryText = Colors.white;
  static const Color secondaryText = Color(0xFF8E96A0);
  
  // Revolut Business Primary Accent (White for buttons, High contrast)
  static const Color accent = Colors.white;
  
  // Revolut Signature Blue (Use for highlights, secondary buttons, active states)
  static const Color primaryBlue = Color(0xFFC9992A); // Premium Royal Gold (Sophisticated and high-contrast)
  
  // Original Revolut Blue — used for Remind button and Priority-2 (approaching) badges
  static const Color infoBlue = Color(0xFF0075FF);
  
  // Border and dividers
  static const Color border = Color(0xFF1F2937);
  
  // Semantic colors
  static const Color success = Color(0xFF00D34D);
  static const Color error = Color(0xFFFF4D4D);
  static const Color warning = Color(0xFFC9992A);

  // Elite Emerald Green shade (#00D34D)
  static const Color emerald = Color(0xFF00D34D);
}

